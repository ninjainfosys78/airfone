---
import BaseLayout from '../../layouts/BaseLayout.astro';
import {
  Waves, Workflow, Route, Database, PhoneForwarded, Voicemail,
  Radio, Phone, RefreshCw, AudioWaveform, BellOff,
  LayoutDashboard, Brain, Mic2, Plug2, Tags
} from 'lucide-react';
const locale = 'ne';
---
<BaseLayout title="सुविधाहरू — स्मार्ट IVR AI" solidNav={true}>

  <!-- ============ HERO + TABS ============ -->
  <section class="feat-hero">
    <div class="section-container">
      <h1 class="feat-hero-title">प्रत्येक क्षमता, तपाईंले वास्तवमा प्रयोग गर्ने तरिका अनुसार व्यवस्थित।</h1>
      <p class="feat-hero-lede">इनबाउन्ड सेवा, आउटबाउन्ड अभियानहरू र ती सबैलाई जोड्ने एनालिटिक्स तह कभर गर्ने तीन मोड्युलहरू।</p>

      <div class="tabs-nav">
        <button class="tab-btn active" data-tab="tab-a">मोड्युल A · स्मार्ट IVR</button>
        <button class="tab-btn" data-tab="tab-b">मोड्युल B · ब्रोडकास्टिङ</button>
        <button class="tab-btn" data-tab="tab-c">मोड्युल C · एनालिटिक्स</button>
      </div>
    </div>
  </section>

  <!-- ============ MODULE A ============ -->
  <section class="module-panel active" id="tab-a">
    <div class="section-container">
      <div class="module-head">
        <span class="module-badge badge-a">मोड्युल A</span>
        <h2>स्मार्ट IVR प्रणाली</h2>
      </div>
      <p class="module-sub">कन्भर्सेसनल रिसेप्शन, डाइनामिक मेनू र स्व-सेवा — सबै प्राकृतिक भाषामा।</p>

      <div class="feat-grid">
        <div class="feat-card">
          <div class="feat-icon icon-a"><Waves size={20} /></div>
          <h3>कन्भर्सेसनल भ्वाइस AI र NLP</h3>
          <p>प्राकृतिक बोली बुझ्दछ, बहु-चरण सम्वादमा सन्दर्भ सुरक्षित राख्छ, ७००ms भन्दा कममा प्रतिक्रिया।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-a"><Workflow size={20} /></div>
          <h3>बहु-तह डाइनामिक मेनू</h3>
          <p>ड्र्याग-एण्ड-ड्रप फ्लो बिल्डर, समय-आधारित राउटिङ, कस्टम न्युरल TTS शुभकामनाहरू।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-a"><Route size={20} /></div>
          <h3>प्रिडिक्टिभ र स्किल राउटिङ</h3>
          <p>स्किल-ट्याग म्याचिङ, VIP प्राथमिकता क्युइङ, राउन्ड-रोबिन र रिङ-अल एल्गोरिदमहरू।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-a"><Database size={20} /></div>
          <h3>AI स्व-सेवा र DB क्वेरी</h3>
          <p>रियल-टाइम खाता खोज, OTP प्रमाणीकरण, नियमित कार्यका लागि शून्य-एजेन्ट समाधान।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-a"><PhoneForwarded size={20} /></div>
          <h3>वार्म/कोल्ड ट्रान्सफर र कन्फरेन्सिङ</h3>
          <p>स्क्रीन-पप वार्म ट्रान्सफर, ब्लाइन्ड ट्रान्सफर, ३-वे म्यानेजर कन्फरेन्सिङ।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-a"><Voicemail size={20} /></div>
          <h3>भोइसमेल-टु-टेक्स्ट र समय बाहिर</h3>
          <p>इमेल/SMS मार्फत स्वचालित ट्रान्सक्रिप्शन, होल्ड संगीतको साटो कलब्याक तालिका।</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ MODULE B ============ -->
  <section class="module-panel" id="tab-b">
    <div class="section-container">
      <div class="module-head">
        <span class="module-badge badge-b">मोड्युल B</span>
        <h2>AI भ्वाइस ब्रोडकास्टिङ र स्मार्ट क्याम्पेन</h2>
      </div>
      <p class="module-sub">पहिलो डायलदेखि नै अनुपालनका साथ ठूलो मात्रामा आउटबाउन्ड पहुँच।</p>

      <div class="feat-grid">
        <div class="feat-card">
          <div class="feat-icon icon-b"><Radio size={20} /></div>
          <h3>सामूहिक भ्वाइस र AI अडियो क्याम्पेन</h3>
          <p>हजारौँ सम्पर्कहरू सम्म पुग्नुहोस्; स्क्रिप्ट टाइप गर्नुहोस् र तुरुन्तै ब्रोडकास्ट अडियो उत्पन्न गर्नुहोस्।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-b"><Phone size={20} /></div>
          <h3>इन्टरएक्टिभ DTMF र भ्वाइस रेस्पोन्स</h3>
          <p>किप्याड वा मौखिक जवाफ, तुरुन्तै "Press-1" लाइभ एजेन्ट स्थानान्तरण सहित।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-b"><RefreshCw size={20} /></div>
          <h3>प्रिडिक्टिभ डायलर र अटो-रिट्राइ</h3>
          <p>डाइनामिक पेसिङ एल्गोरिदम, कन्फिगर योग्य पुन: प्रयास नियमहरू, उत्तर दिने मेसिन पहिचान।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-b"><AudioWaveform size={20} /></div>
          <h3>डाइनामिक AI टेक्स्ट-टु-स्पीच</h3>
          <p>व्यक्तिगतकरणका लागि भेरिएबल इन्जेक्शन, स्थानीय नेपाली/अंग्रेजी/हिन्दी भ्वाइस मोडलहरू।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-b"><BellOff size={20} /></div>
          <h3>DND र अनुपालन फिल्टरिङ</h3>
          <p>नियमन सूचीहरू विरुद्ध स्वचालित DND स्क्रबिङ, एक-कीप्रेस अप्ट-आउट।</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ MODULE C ============ -->
  <section class="module-panel" id="tab-c">
    <div class="section-container">
      <div class="module-head">
        <span class="module-badge badge-c">मोड्युल C</span>
        <h2>AI एनालिटिक्स, सुपरभिजन र इन्टिग्रेसन</h2>
      </div>
      <p class="module-sub">सुपरभाइजर र CRM दुवैलाई चाहिने सबै कुरा, रियल टाइममा।</p>

      <div class="feat-grid">
        <div class="feat-card">
          <div class="feat-icon icon-c"><LayoutDashboard size={20} /></div>
          <h3>लाइभ सुपरभिजन ड्यासबोर्ड</h3>
          <p>वालबोर्ड मेट्रिक्स, साथै सुपरभाइजरहरूका लागि ह्विस्पर, बार्ज-इन र टेकओभर नियन्त्रण।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-c"><Brain size={20} /></div>
          <h3>भावना र उद्देश्य पहिचान</h3>
          <p>हरेक कललाई सकारात्मक/तथस्थ/नकारात्मक ग्रेडिङ गर्छ र एस्कलेसन स्वतः फ्ल्याग गर्छ।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-c"><Mic2 size={20} /></div>
          <h3>दोहोरो-च्यानल रेकर्डिङ</h3>
          <p>छुट्टाछुट्टै ग्राहक/एजेन्ट च्यानलहरू, अनुपालन लेखापरीक्षणका लागि पूर्ण खोजयोग्य ट्रान्सक्रिप्टहरू।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-c"><Plug2 size={20} /></div>
          <h3>REST API र वेबहुक गेटवे</h3>
          <p>Salesforce, HubSpot, Zoho का लागि पूर्व-निर्मित कनेक्टर्स, साथै दुई-तर्फी वेबहुकहरू।</p>
        </div>
        <div class="feat-card">
          <div class="feat-icon icon-c"><Tags size={20} /></div>
          <h3>स्वचालित सारांश र ट्यागिङ</h3>
          <p>AI-द्वारा लिखित कल सारांश र विषय अनुसार स्वचालित ट्यागिङ।</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ ARCHITECTURE STRIP ============ -->
  <section class="mini-arch-section">
    <div class="section-container">
      <span class="section-eyebrow">आर्किटेक्चर</span>
      <h2 class="mini-arch-title">चार मुख्य आर्किटेक्चरल तहहरू।</h2>
      <p class="module-sub">क्लाउड-नेटिभ, माइक्रोसर्भिसमा आधारित, सब-सेकेन्ड अडियो विलम्बताका लागि निर्मित।</p>

      <div class="mini-arch-grid">
        <div class="mini-arch-card">
          <h4>०१ · टेलिफोनी र एज</h4>
          <p>SBCs र WebRTC गेटवेहरू NTC, Ncell र अन्तर्राष्ट्रिय अपरेटरहरूसँग जोडिन्छन्।</p>
        </div>
        <div class="mini-arch-card">
          <h4>०२ · AI अर्केस्ट्रेशन</h4>
          <p>७००ms भन्दा कममा प्रतिक्रियाका लागि STT, NLU, र न्युरल TTS समन्वय गर्छ।</p>
        </div>
        <div class="mini-arch-card">
          <h4>०३ · एप्लिकेसन र DB</h4>
          <p>बिजनेस लजिक र लाइभ अनुगमनका लागि मल्टि-टेनेन्ट क्लस्टर।</p>
        </div>
        <div class="mini-arch-card">
          <h4>०४ · इन्टिग्रेसन गेटवे</h4>
          <p>CRMs, ERPs र कस्टम डाटाबेसहरूका लागि REST API र वेबहुक मिडलवेयर।</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ CTA ============ -->
  <section class="feat-cta-section">
    <div class="feat-cta-inner">
      <div class="feat-cta-text">
        <h2>वास्तविक लाइनमा यी मोड्युलहरू हेर्नुहोस्।</h2>
        <p>हामी इनबाउन्ड र आउटबाउन्ड प्रवाहहरू प्रत्यक्ष रूपमा देखाउनेछौँ।</p>
      </div>
      <a href={`/${locale}/contact/`} class="feat-cta-btn">लाइभ डेमो बुक गर्नुहोस्</a>
    </div>
  </section>

</BaseLayout>

<script>
  function initFeaturesTabs() {
    const tabBtns = document.querySelectorAll('.tab-btn');
    const panels = document.querySelectorAll('.module-panel');

    function activateTab(tabId: string) {
      tabBtns.forEach(btn => {
        const isTarget = btn.getAttribute('data-tab') === tabId;
        btn.classList.toggle('active', isTarget);
      });
      panels.forEach(panel => {
        const isTarget = panel.getAttribute('id') === tabId;
        panel.classList.toggle('active', isTarget);
      });
    }

    tabBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        const tabId = btn.getAttribute('data-tab');
        if (tabId) {
          activateTab(tabId);
        }
      });
    });

    const hash = window.location.hash.replace('#', '');
    if (hash && document.getElementById(hash)) {
      activateTab(hash);
    }
  }

  initFeaturesTabs();
  document.addEventListener('astro:page-load', initFeaturesTabs);
</script>

<style>
  /* ============ SHARED ============ */
  .section-container { max-width: 1280px; margin: 0 auto; padding: 0 2rem; }
  .section-eyebrow {
    display: block; font-family: monospace; font-size: 0.78rem; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase; color: var(--primary-dark); margin-bottom: 1rem;
  }
  .module-sub { font-size: 0.98rem; color: var(--text-secondary); line-height: 1.6; margin: 0 0 2.5rem; max-width: 640px; }

  /* ============ HERO + TABS ============ */
  .feat-hero { background: #FFFFFF; padding: 11rem 0 3rem; }
  .feat-hero-title {
    font-size: clamp(1.9rem, 4vw, 2.7rem); font-weight: 800; line-height: 1.2;
    color: var(--text-primary); letter-spacing: -0.02em; margin: 0 0 1rem; max-width: 780px;
  }
  .feat-hero-lede { font-size: 1.05rem; color: var(--text-secondary); line-height: 1.65; max-width: 640px; margin: 0 0 2.5rem; }

  .tabs-nav { display: flex; gap: 2rem; border-bottom: 1px solid var(--border-color); }
  .tab-btn {
    background: none; border: none; cursor: pointer; padding: 0 0 1rem;
    font-size: 0.95rem; font-weight: 600; color: var(--text-secondary);
    border-bottom: 2px solid transparent; margin-bottom: -1px; transition: color 0.2s;
  }
  .tab-btn.active { color: var(--primary-dark); border-bottom-color: var(--primary); }
  .tab-btn:hover:not(.active) { color: var(--text-primary); }

  /* ============ MODULE PANELS ============ */
  .module-panel { display: none; background: var(--bg-secondary); padding: 3.5rem 0; }
  .module-panel.active { display: block; }

  .module-head { display: flex; align-items: center; gap: 0.9rem; margin-bottom: 0.6rem; }
  .module-head h2 { font-size: 1.5rem; font-weight: 800; color: var(--text-primary); margin: 0; }

  .module-badge {
    font-family: monospace; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.04em;
    padding: 0.3rem 0.7rem; border-radius: 6px; color: #FFFFFF;
  }
  .badge-a { background: linear-gradient(135deg, #6DC040, #2A8A3C); }
  .badge-b { background: linear-gradient(135deg, #C9862E, #8A5A18); }
  .badge-c { background: linear-gradient(135deg, #3DB551, #1B5E20); }

  .feat-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; }

  .feat-card {
    background: #FFFFFF; border: 1px solid var(--border-color); border-radius: 14px;
    padding: 1.75rem; transition: transform 0.25s ease, box-shadow 0.25s ease;
  }
  .feat-card:hover { transform: translateY(-4px); box-shadow: 0 10px 24px rgba(61, 181, 81, 0.12); }

  .feat-icon {
    width: 42px; height: 42px; border-radius: 10px; display: flex; align-items: center; justify-content: center;
    margin-bottom: 1rem;
  }
  .icon-a { background: rgba(61, 181, 81, 0.1); color: var(--primary-dark); }
  .icon-b { background: rgba(201, 134, 46, 0.12); color: #A5691F; }
  .icon-c { background: rgba(61, 181, 81, 0.1); color: var(--primary-dark); }

  .feat-card h3 { font-size: 1rem; font-weight: 700; color: var(--text-primary); margin: 0 0 0.5rem; }
  .feat-card p { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.6; margin: 0; }

  /* ============ ARCHITECTURE STRIP ============ */
  .mini-arch-section { background: #FFFFFF; padding: 4.5rem 0; }
  .mini-arch-title { font-size: clamp(1.5rem, 3vw, 2rem); font-weight: 800; color: var(--text-primary); margin: 0 0 0.75rem; }
  .mini-arch-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem; }
  .mini-arch-card { background: var(--bg-secondary); border: 1px solid var(--border-color); border-radius: 14px; padding: 1.5rem; }
  .mini-arch-card h4 { font-size: 0.95rem; font-weight: 700; color: var(--text-primary); margin: 0 0 0.5rem; }
  .mini-arch-card p { font-size: 0.85rem; color: var(--text-secondary); line-height: 1.6; margin: 0; }

  /* ============ CTA ============ */
  .feat-cta-section {
    background: linear-gradient(180deg, #E4F5D4 0%, #F2FAE6 55%, #F8FCF2 100%);
    padding: 4rem 2.5rem 5rem;
  }
  .feat-cta-inner {
    max-width: 1280px; margin: 0 auto;
    background: #FFFFFF;
    border: 1px solid rgba(61, 181, 81, 0.25); border-radius: 16px; padding: 2.5rem 3rem;
    display: flex; align-items: center; justify-content: space-between; gap: 2rem; flex-wrap: wrap;
    box-shadow: 0 8px 24px rgba(61, 181, 81, 0.15);
  }
  .feat-cta-text h2 { font-size: 1.5rem; font-weight: 700; color: #1A3A1A; margin: 0 0 0.4rem; }
  .feat-cta-text p { font-size: 0.9rem; color: rgba(26, 58, 26, 0.7); margin: 0; }
  .feat-cta-btn {
    display: inline-flex; align-items: center; padding: 0.8rem 1.6rem;
    background: linear-gradient(135deg, #6DC040, #2A8A3C); color: #FFFFFF; font-weight: 700;
    font-size: 0.9rem; border-radius: 10px; text-decoration: none; white-space: nowrap;
    box-shadow: 0 3px 12px rgba(61, 181, 81, 0.35); transition: all 0.2s ease;
  }
  .feat-cta-btn:hover { background: linear-gradient(135deg, #3DB551, #1B7A2F); color: #FFFFFF; transform: translateY(-1px); }

  @media (max-width: 900px) {
    .feat-grid { grid-template-columns: repeat(2, 1fr); }
    .mini-arch-grid { grid-template-columns: repeat(2, 1fr); }
    .tabs-nav { gap: 1.25rem; overflow-x: auto; }
  }
  @media (max-width: 600px) {
    .feat-hero { padding: 9rem 0 2rem; }
    .feat-grid { grid-template-columns: 1fr; }
    .mini-arch-grid { grid-template-columns: 1fr; }
    .feat-cta-section { padding: 3rem 1.25rem 3rem; }
    .feat-cta-inner { flex-direction: column; align-items: flex-start; padding: 2rem 1.5rem; }
    .feat-cta-btn { width: 100%; text-align: center; justify-content: center; }
  }

  /* Dark mode — deep/medium green sections */
  html[data-theme="dark"] .feat-hero {
    background: var(--bg-primary);
  }

  html[data-theme="dark"] .feat-card {
    background: var(--card-bg);
    border-color: var(--card-border);
  }

  html[data-theme="dark"] .feat-card:hover {
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.4);
  }

  html[data-theme="dark"] .icon-b {
    color: #E0A94F;
  }

  html[data-theme="dark"] .mini-arch-section {
    background: var(--bg-primary);
  }

  html[data-theme="dark"] .mini-arch-card {
    background: var(--card-bg);
    border-color: var(--card-border);
  }

  html[data-theme="dark"] .feat-cta-section {
    background: linear-gradient(180deg, #0A2410 0%, #0C2B14 55%, #0F2E1A 100%);
  }

  html[data-theme="dark"] .feat-cta-inner {
    background: var(--card-bg);
    border-color: rgba(109, 192, 64, 0.3);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
  }

  html[data-theme="dark"] .feat-cta-text h2 {
    color: var(--text-primary);
  }

  html[data-theme="dark"] .feat-cta-text p {
    color: var(--text-secondary);
  }
</style>
