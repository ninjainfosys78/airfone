---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Check, BadgeDollarSign, ShieldCheck, Phone, ArrowRight } from 'lucide-react';
const locale = 'ne';
---

<BaseLayout title="मूल्य निर्धारण — AirFone" solidNav={true}>

  <!-- ============ HERO ============ -->
  <section class="page-hero">
    <div class="page-hero-bg" aria-hidden="true"></div>
    <div class="page-hero-inner">
      <div class="page-hero-text">
        <h1 class="page-hero-title">
          पारदर्शी तहहरू।<br />
          <span class="page-hero-highlight">कुनै आश्चर्यजनक मिनेट छैन।</span>
        </h1>
        <p class="page-hero-lede">
          एकपटकको सेटअप, तपाईंको कल भोल्युमसँग मिल्ने मासिक सदस्यता, र प्रसारण
          मिनेटहरूका लागि पे-एज-यु-गो दरहरू।
        </p>
        <div class="page-hero-chips">
          <span class="page-hero-chip"><Check size={14} /> पारदर्शी NPR मूल्य</span>
          <span class="page-hero-chip"><Check size={14} /> पे-एज-यु-गो मिनेट</span>
          <span class="page-hero-chip"><Check size={14} /> कुनै लुकेको शुल्क छैन</span>
        </div>
      </div>

      <div class="page-hero-visual" aria-hidden="true">
        <div class="page-hero-ring">
          <div class="page-hero-ring-circle page-hero-ring-c1"></div>
          <div class="page-hero-ring-circle page-hero-ring-c2"></div>
          <div class="page-hero-ring-circle page-hero-ring-c3"></div>
          <div class="page-hero-ring-phone"><BadgeDollarSign size={42} /></div>
        </div>
        <div class="page-hero-float-card">
          <div class="page-hero-float-icon"><ShieldCheck size={18} /></div>
          <div>
            <div class="page-hero-float-num">९९.५%+</div>
            <div class="page-hero-float-label">सबै प्लानमा SLA</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ SETUP FEE ============ -->
  <section class="setup-section">
    <div class="section-container">
      <h2 class="section-title">एकपटकको सेटअप शुल्क</h2>

      <div class="setup-table">
        <div class="setup-row setup-head">
          <span>प्याकेज</span><span>समावेश गर्दछ</span><span>शुल्क</span>
        </div>
        <div class="setup-row">
          <span class="setup-name">स्ट्यान्डर्ड क्लाउड सेटअप</span>
          <span>मल्टि-टेनेन्ट क्लाउड अनबोर्डिङ, SIP ट्रंक कन्फिग, आधारभूत IVR फ्लो</span>
          <span class="setup-fee">NPR ३०,०००</span>
        </div>
        <div class="setup-row">
          <span class="setup-name">एन्टरप्राइज / AI कस्टम सेटअप</span>
          <span>डेडिकेटेड क्लस्टर, कस्टम AI फ्लो डिजाइन, CRM इन्टिग्रेसन बिल्ड</span>
          <span class="setup-fee">NPR ५०,०००</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ PLANS ============ -->
  <section class="plans-section">
    <div class="section-container">
      <span class="section-eyebrow">सदस्यता</span>
      <h2 class="section-title">मासिक प्लानहरू</h2>

      <div class="plans-grid">
        <div class="plan-card">
          <span class="plan-name">Starter</span>
          <div class="plan-price">NPR ४,५००<span>/मास</span></div>
          <ul class="plan-specs">
            <li><span>च्यानलहरू</span><b>१० समवर्ती</b></li>
            <li><span>एजेन्ट एक्सटेन्सन</span><b>५</b></li>
            <li><span>समावेश मिनेट</span><b>२,०००</b></li>
            <li><span>IVR प्रकार</span><b>स्ट्यान्डर्ड मेनु</b></li>
            <li><span>AI क्षमता</span><b>आधारभूत NLU</b></li>
            <li><span>रेकर्डिङ अवधि</span><b>३० दिन</b></li>
            <li><span>SLA अपटाइम</span><b>९९.५%</b></li>
          </ul>
          <a href={`/${locale}/demo/`} class="plan-btn plan-btn-outline">Starter छान्नुहोस्</a>
        </div>

        <div class="plan-card plan-featured">
          <span class="plan-ribbon">सिफारिस गरिएको</span>
          <span class="plan-name">Growth</span>
          <div class="plan-price">NPR ९,५००<span>/मास</span></div>
          <ul class="plan-specs">
            <li><span>च्यानलहरू</span><b>५० समवर्ती</b></li>
            <li><span>एजेन्ट एक्सटेन्सन</span><b>२५</b></li>
            <li><span>समावेश मिनेट</span><b>१०,०००</b></li>
            <li><span>IVR प्रकार</span><b>डाइनामिक + सेल्फ-सर्भिस</b></li>
            <li><span>AI क्षमता</span><b>Conversational AI</b></li>
            <li><span>रेकर्डिङ अवधि</span><b>९० दिन</b></li>
            <li><span>SLA अपटाइम</span><b>९९.९%</b></li>
          </ul>
          <a href={`/${locale}/demo/`} class="plan-btn plan-btn-filled">Growth छान्नुहोस्</a>
        </div>

        <div class="plan-card">
          <span class="plan-name">AI Enterprise</span>
          <div class="plan-price">NPR २२,५००<span>/मास</span></div>
          <ul class="plan-specs">
            <li><span>च्यानलहरू</span><b>१,०००+ समवर्ती</b></li>
            <li><span>एजेन्ट एक्सटेन्सन</span><b>असीमित</b></li>
            <li><span>समावेश मिनेट</span><b>५०,०००</b></li>
            <li><span>IVR प्रकार</span><b>पूर्ण AI Orchestration</b></li>
            <li><span>AI क्षमता</span><b>Sentiment + Analytics</b></li>
            <li><span>रेकर्डिङ अवधि</span><b>३६५ दिन</b></li>
            <li><span>SLA अपटाइम</span><b>९९.९५%</b></li>
          </ul>
          <a href={`/${locale}/contact/`} class="plan-btn plan-btn-outline">बिक्रीसँग कुरा गर्नुहोस्</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ PAYG RATES ============ -->
  <section class="payg-section">
    <div class="section-container">
      <span class="section-eyebrow">पे-एज-यु-गो</span>
      <h2 class="section-title">आउटबाउन्ड प्रसारण र TTS दरहरू</h2>

      <div class="payg-table">
        <div class="payg-row payg-head">
          <span>प्रयोग तह</span><span>मासिक मिनेट</span><span>प्रसारण दर</span><span>डाइनामिक TTS दर</span>
        </div>
        <div class="payg-row">
          <span class="payg-tier">Tier 1</span><span>१०,००० मिनेट सम्म</span><span class="payg-rate">NPR १.२०/मिनेट</span><span class="payg-rate">NPR १.५०/मिनेट</span>
        </div>
        <div class="payg-row">
          <span class="payg-tier">Tier 2</span><span>१०,०००–५०,००० मिनेट</span><span class="payg-rate">NPR १.००/मिनेट</span><span class="payg-rate">NPR १.२५/मिनेट</span>
        </div>
        <div class="payg-row">
          <span class="payg-tier">Tier 3</span><span>५०,०००+ मिनेट</span><span class="payg-rate">NPR ०.८०/मिनेट</span><span class="payg-rate">NPR १.००/मिनेट</span>
        </div>
      </div>

      <span class="section-eyebrow addon-eyebrow">थप-सुविधा क्रेडिट</span>
      <div class="addon-table">
        <div class="addon-row addon-head"><span>थप-सुविधा</span><span>दर</span></div>
        <div class="addon-row"><span>स्वचालित ट्रान्सक्रिप्सन</span><span class="payg-rate">NPR ०.५०/मिनेट</span></div>
        <div class="addon-row"><span>सेन्टिमेन्ट विश्लेषण</span><span class="payg-rate">NPR ०.३०/कल</span></div>
        <div class="addon-row"><span>SMS फलब्याक</span><span class="payg-rate">NPR १.००/SMS</span></div>
      </div>
      <p class="vat-note">सबै NPR मूल्यमा VAT समावेश छैन।</p>
    </div>
  </section>

  <!-- ============ CTA ============ -->
  <section class="cta-section">
    <div class="cta-card">
      <div class="cta-glow" aria-hidden="true"></div>
      <div class="cta-content">
        <h2 class="cta-title">कुन तह तपाईंको कल भोल्युमसँग <span class="cta-accent">मिल्छ?</span></h2>
        <p class="cta-lede">हामीलाई तपाईंको औसत समवर्ती कल पठाउनुहोस् र हामी एउटा प्लान सिफारिस गर्नेछौं।</p>
        <div class="cta-actions">
          <a href={`/${locale}/contact/`} class="cta-btn">
            बिक्रीसँग कुरा गर्नुहोस्
            <ArrowRight size={18} />
          </a>
        </div>
      </div>

      <div class="cta-visual" aria-hidden="true">
        <div class="cta-ring">
          <div class="cta-ring-c1"></div>
          <div class="cta-ring-c2"></div>
          <div class="cta-ring-c3"></div>
          <div class="cta-ring-phone"><Phone size={40} /></div>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  .section-container { max-width: 1280px; margin: 0 auto; padding: 0 2rem; }
  .section-eyebrow {
    display: block; font-family: monospace; font-size: 0.78rem; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase; color: var(--primary-dark); margin-bottom: 1rem;
  }
  .section-title { font-size: clamp(1.6rem, 3vw, 2.1rem); font-weight: 800; color: var(--text-primary); letter-spacing: -0.01em; line-height: 1.4; margin: 0 0 2rem; }

  /* ============ SETUP TABLE ============ */
  .setup-section { background: #FFFFFF; padding: 4.5rem 0; }
  .setup-table { border: 1px solid var(--border-color); border-radius: 14px; overflow: hidden; }
  .setup-row {
    display: grid; grid-template-columns: 1.2fr 2fr 1fr; gap: 1.5rem; padding: 1.25rem 1.5rem;
    border-bottom: 1px solid var(--border-color); align-items: center;
  }
  .setup-row:last-child { border-bottom: none; }
  .setup-head { background: var(--bg-secondary); font-family: monospace; font-size: 0.72rem; letter-spacing: 0.06em; text-transform: uppercase; color: var(--text-secondary); }
  .setup-name { font-weight: 700; color: var(--text-primary); font-size: 0.95rem; }
  .setup-row:not(.setup-head) span:nth-child(2) { font-size: 0.9rem; color: var(--text-secondary); }
  .setup-fee { font-family: monospace; color: var(--primary-dark); font-weight: 700; }

  /* ============ PLANS ============ */
  .plans-section { background: var(--bg-secondary); padding: 4.5rem 0; }
  .plans-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; align-items: start; }

  .plan-card {
    background: #FFFFFF; border: 1px solid var(--border-color); border-radius: 18px;
    padding: 2rem; display: flex; flex-direction: column; position: relative;
  }
  .plan-featured { border: 2px solid var(--primary); box-shadow: 0 12px 30px rgba(61, 181, 81, 0.15); }
  .plan-ribbon {
    position: absolute; top: -13px; left: 2rem; background: var(--primary-dark); color: #fff;
    font-family: monospace; font-size: 0.7rem; padding: 0.3rem 0.8rem; border-radius: 9999px; letter-spacing: 0.04em;
  }
  .plan-name { font-family: monospace; font-size: 0.75rem; letter-spacing: 0.06em; text-transform: uppercase; color: var(--text-secondary); }
  .plan-price { font-size: 2rem; font-weight: 800; color: var(--text-primary); margin: 0.6rem 0 1.5rem; }
  .plan-price span { font-size: 0.9rem; font-weight: 500; color: var(--text-secondary); }

  .plan-specs { list-style: none; margin: 0 0 1.5rem; padding: 0; flex: 1; }
  .plan-specs li { display: flex; justify-content: space-between; gap: 0.75rem; padding: 0.6rem 0; border-bottom: 1px solid var(--border-color); font-size: 0.85rem; }
  .plan-specs li:first-child { border-top: 1px solid var(--border-color); }
  .plan-specs li span { color: var(--text-secondary); }
  .plan-specs li b { color: var(--text-primary); font-weight: 700; text-align: right; }

  .plan-btn { display: block; text-align: center; padding: 0.85rem; border-radius: 10px; font-weight: 700; font-size: 0.9rem; text-decoration: none; }
  .plan-btn-outline { border: 1.5px solid var(--border-color); color: var(--text-primary); }
  .plan-btn-outline:hover { border-color: var(--primary); color: var(--primary-dark); }
  .plan-btn-filled { background: linear-gradient(135deg, #6DC040, #2A8A3C); color: #FFFFFF; box-shadow: 0 3px 12px rgba(61, 181, 81, 0.3); }
  .plan-btn-filled:hover { background: linear-gradient(135deg, #3DB551, #1B7A2F); color: #FFFFFF; }

  /* ============ PAYG + ADD-ON TABLES ============ */
  .payg-section { background: #FFFFFF; padding: 4.5rem 0; }
  .payg-table, .addon-table { border: 1px solid var(--border-color); border-radius: 14px; overflow: hidden; margin-bottom: 1rem; }
  .payg-row { display: grid; grid-template-columns: 1fr 1.4fr 1fr 1fr; gap: 1rem; padding: 1.1rem 1.5rem; border-bottom: 1px solid var(--border-color); align-items: center; }
  .addon-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; padding: 1.1rem 1.5rem; border-bottom: 1px solid var(--border-color); align-items: center; }
  .payg-row:last-child, .addon-row:last-child { border-bottom: none; }
  .payg-head, .addon-head { background: var(--bg-secondary); font-family: monospace; font-size: 0.72rem; letter-spacing: 0.06em; text-transform: uppercase; color: var(--text-secondary); }
  .payg-tier { font-weight: 700; color: var(--text-primary); }
  .payg-rate { font-family: monospace; color: var(--primary-dark); font-weight: 700; }
  .addon-eyebrow { margin-top: 3rem; }
  .vat-note { font-size: 0.8rem; color: var(--text-secondary); opacity: 0.7; margin: 0.5rem 0 0; }

  /* ============ CTA ============ */
  .pricing-cta-section {
    background: linear-gradient(180deg, #E4F5D4 0%, #F2FAE6 55%, #F8FCF2 100%);
    padding: 4rem 2.5rem 5rem;
  }
  .pricing-cta-inner {
    max-width: 1280px; margin: 0 auto;
    background: #FFFFFF;
    border: 1px solid rgba(61, 181, 81, 0.25); border-radius: 16px; padding: 2.5rem 3rem;
    display: flex; align-items: center; justify-content: space-between; gap: 2rem; flex-wrap: wrap;
    box-shadow: 0 8px 24px rgba(61, 181, 81, 0.15);
  }
  .pricing-cta-text h2 { font-size: 1.5rem; font-weight: 700; color: #1A3A1A; margin: 0 0 0.4rem; }
  .pricing-cta-text p { font-size: 0.9rem; color: rgba(26, 58, 26, 0.7); margin: 0; }
  .pricing-cta-btn {
    display: inline-flex; align-items: center; padding: 0.8rem 1.6rem;
    background: linear-gradient(135deg, #6DC040, #2A8A3C); color: #FFFFFF; font-weight: 700;
    font-size: 0.9rem; border-radius: 10px; text-decoration: none; white-space: nowrap;
    box-shadow: 0 3px 12px rgba(61, 181, 81, 0.35); transition: all 0.2s ease;
  }
  .pricing-cta-btn:hover { background: linear-gradient(135deg, #3DB551, #1B7A2F); color: #FFFFFF; transform: translateY(-1px); }

  @media (max-width: 900px) {
    .plans-grid { grid-template-columns: 1fr; }
    .setup-row { grid-template-columns: 1fr; gap: 0.4rem; }
    .payg-row { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 600px) {
    .addon-row, .payg-row { grid-template-columns: 1fr; gap: 0.3rem; }
    .pricing-cta-section { padding: 3rem 1.25rem 3rem; }
    .pricing-cta-inner { flex-direction: column; align-items: flex-start; padding: 2rem 1.5rem; }
    .pricing-cta-btn { width: 100%; text-align: center; justify-content: center; }
  }

  /* Dark mode — deep/medium green sections */
  html[data-theme="dark"] .setup-section {
    background: var(--bg-primary);
  }

  html[data-theme="dark"] .plans-section {
    background: var(--bg-secondary);
  }

  html[data-theme="dark"] .plan-card {
    background: var(--card-bg);
    border-color: var(--card-border);
  }

  html[data-theme="dark"] .plan-featured {
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.4);
  }

  html[data-theme="dark"] .payg-section {
    background: var(--bg-primary);
  }

  /* ============ PRE-FOOTER CTA SECTION ============ */
  .cta-section {
    background: linear-gradient(180deg, #FFFFFF 0%, #F3F9EE 100%);
    padding: 1rem 2.5rem 5rem;
  }
  .cta-card {
    position: relative; overflow: hidden;
    max-width: 1280px; margin: 0 auto;
    background: linear-gradient(135deg, #0B3318 0%, #16612B 55%, #2A8A3C 100%);
    border-radius: 24px; padding: 3.5rem;
    display: grid; grid-template-columns: 1fr auto;
    align-items: center; gap: 2.5rem;
  }
  .cta-glow {
    position: absolute; top: -55%; left: -15%;
    width: 500px; height: 500px;
    background: radial-gradient(circle, rgba(109, 192, 64, 0.45) 0%, rgba(109, 192, 64, 0) 70%);
    pointer-events: none;
  }
  .cta-content { position: relative; z-index: 1; }
  .cta-title {
    font-size: clamp(1.8rem, 3.6vw, 2.6rem);
    font-weight: 800; line-height: 1.2; letter-spacing: -0.02em;
    color: #FFFFFF; margin: 0 0 1rem; max-width: 640px;
  }
  .cta-accent { color: #8FE07C; }
  .cta-lede {
    font-size: 1rem; line-height: 1.65; color: rgba(255, 255, 255, 0.8);
    max-width: 560px; margin: 0 0 2rem;
  }
  .cta-actions { display: flex; align-items: center; gap: 1.5rem; flex-wrap: wrap; }
  .cta-btn {
    display: inline-flex; align-items: center; gap: 0.6rem;
    padding: 0.95rem 1.9rem;
    background: #FFFFFF; color: #145A27; font-weight: 800;
    font-size: 0.95rem; border-radius: 12px; text-decoration: none; white-space: nowrap;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.25);
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
  }
  .cta-btn:hover { background: #F2FAEC; color: #0B3318; transform: translateY(-2px); box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3); }
  .cta-visual { position: relative; width: 220px; height: 220px; z-index: 1; }
  .cta-ring { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; }
  .cta-ring-c1, .cta-ring-c2, .cta-ring-c3 {
    position: absolute; border-radius: 50%;
    border: 1px solid rgba(255, 255, 255, 0.22);
    animation: ctaPulse 3s ease-out infinite;
  }
  .cta-ring-c1 { width: 220px; height: 220px; }
  .cta-ring-c2 { width: 150px; height: 150px; animation-delay: 0.6s; }
  .cta-ring-c3 { width: 90px; height: 90px; animation-delay: 1.2s; }
  .cta-ring-phone {
    width: 64px; height: 64px; border-radius: 50%;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.35);
    color: #FFFFFF; display: flex; align-items: center; justify-content: center;
  }
  @keyframes ctaPulse {
    0% { transform: scale(0.85); opacity: 0; }
    40% { opacity: 1; }
    100% { transform: scale(1.15); opacity: 0; }
  }
  @media (max-width: 900px) {
    .cta-card { grid-template-columns: 1fr; text-align: center; }
    .cta-actions { justify-content: center; }
    .cta-visual { display: none; }
  }
  @media (max-width: 600px) {
    .cta-section { padding: 0.5rem 1.25rem 3rem; }
    .cta-card { padding: 2.5rem 1.5rem; }
    .cta-actions { flex-direction: column; }
    .cta-btn { width: 100%; justify-content: center; }
  }
  html[data-theme="dark"] .cta-section {
    background: linear-gradient(180deg, var(--bg-primary) 0%, #08170C 100%);
  }
  html[data-theme="dark"] .cta-btn:hover { background: #F2FAEC; }
</style>