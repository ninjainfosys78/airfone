---
// Required by Astro's i18n routing as the redirect source for the
// default locale. With `prefixDefaultLocale: true` + `redirectToDefaultLocale: true`,
// visiting `/` 302s to `/en/`. Astro reads this file to know there's
// a homepage to anchor the redirect against; it never renders.
return Astro.redirect('/en/');
---
